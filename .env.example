# Loggarr Configuration

# Web UI port
PORT=9797

# Prometheus metrics port
METRICS_PORT=9091

# Number of log lines to keep in memory (not persisted)
LOG_BUFFER_SIZE=100

# Maximum configurable buffer size
LOG_BUFFER_MAX=1000

# Data directory for SQLite (snapshots, config)
DATA_DIR=./data
